-- =========================================================================
-- Migration: Z3 — مبلغ روی فیش‌های پرداخت (برای صفحهٔ فاکتورها)
-- اجرا کن: psql $DATABASE_URL -f این_فایل.sql
-- =========================================================================

ALTER TABLE payments
  ADD COLUMN IF NOT EXISTS amount INTEGER;
