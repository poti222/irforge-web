/**
 * lib/auth-policy.ts
 * ─────────────────────────────────────────────────────────────────────────────
 * آینه‌ی api-server/src/lib/authPolicy.ts::EMAIL_AUTH_CLOSED — ورود/ثبت‌نام با
 * ایمیل کاملاً بسته است. سرور خودش هم هر سه مسیر ایمیلی را رد می‌کند؛ این
 * فقط UI را با همان تصمیم هماهنگ نگه می‌دارد تا کاربر اصلاً گزینه‌ی بسته را
 * نبیند، نه این‌که فرم را پر کند و بعد با یک خطای سرور مواجه شود.
 */
export const EMAIL_AUTH_CLOSED = true;
