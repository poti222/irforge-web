/**
 * lib/auth-policy.ts
 * ─────────────────────────────────────────────────────────────────────────────
 * آینه‌ی api-server/src/lib/authPolicy.ts::EMAIL_AUTH_CLOSED. این فقط UI را
 * با همان تصمیمِ سرور هماهنگ نگه می‌دارد تا کاربر گزینه‌ای که سرور رد
 * می‌کند اصلاً نبیند، نه این‌که فرم را پر کند و بعد با یک خطای سرور مواجه
 * شود.
 */
export const EMAIL_AUTH_CLOSED = false;
