/**
 * lib/authPolicy.ts
 * ─────────────────────────────────────────────────────────────────────────────
 * ورود/ثبت‌نام با ایمیل کاملاً بسته است — فقط تلفن/تلگرام باقی می‌ماند. یک
 * ثابتِ واحد به‌جای شرطِ پراکنده در چند مسیر (`routes/auth.ts`،
 * `routes/registration.ts`)، تا اگر روزی لازم شد برگردانده شود، یک تغییرِ
 * یک‌خطی باشد، نه شکارِ همه‌ی جاهایی که این تصمیم را چک می‌کنند.
 *
 * حساب‌های قدیمیِ فقط-ایمیل (بدون تلگرام و بدون شماره) که از قبل ساخته
 * شده‌اند دست‌نخورده می‌مانند — این‌جا فقط *مسیر ورود/ثبت‌نامِ تازه* با ایمیل
 * بسته می‌شود، نه خودِ رکوردها. چنین کاربری برای ورود دوباره باید یا تلگرام
 * وصل کند (`/auth/telegram/link/start` هنوز کار می‌کند چون از قبل authenticated
 * بودن لازم دارد، پس این خودش راه‌حل نیست) یا از طریق پشتیبانی رسیدگی شود؛
 * `/auth/forgot-password` دست‌نخورده مانده چون از اول هم کدش را از تلگرام
 * می‌فرستد، نه ایمیل — «ایمیل» آن‌جا فقط کلیدِ جست‌وجوست.
 */
export const EMAIL_AUTH_CLOSED = true;

export const EMAIL_AUTH_CLOSED_MESSAGE_FA =
  "ورود و ثبت‌نام با ایمیل دیگر در دسترس نیست. لطفاً با شماره تلفن یا تلگرام وارد شوید.";
export const EMAIL_AUTH_CLOSED_CODE = "email_auth_closed";
