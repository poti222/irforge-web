/**
 * lib/authPolicy.ts
 * ─────────────────────────────────────────────────────────────────────────────
 * دروازه‌ی ورود/ثبت‌نام با ایمیل. یک ثابتِ واحد به‌جای شرطِ پراکنده در چند
 * مسیر (`routes/auth.ts`، `routes/registration.ts`) و در فرانت
 * (`irforge/src/lib/auth-policy.ts`، آینه‌ی همین مقدار) — تا بستن یا باز
 * کردنش یک تغییرِ یک‌خطی باشد، نه شکارِ همه‌ی جاهایی که این تصمیم را چک
 * می‌کنند.
 *
 * فعلاً باز است، چون صفحه‌ی ثبت‌نام هر سه راه (پیامک، ایمیل، تلگرام) را کنار
 * هم نشان می‌دهد. این دو همیشه با هم عوض می‌شوند: کسی که با ایمیل ثبت‌نام
 * می‌کند نه تلفنی وصل دارد نه تلگرامی، پس اگر فقط ورودِ ایمیل بسته بماند،
 * چنین کاربری هیچ راهی برای برگشتن به حسابش نخواهد داشت.
 */
export const EMAIL_AUTH_CLOSED = false;

export const EMAIL_AUTH_CLOSED_MESSAGE_FA =
  "ورود و ثبت‌نام با ایمیل دیگر در دسترس نیست. لطفاً با شماره تلفن یا تلگرام وارد شوید.";
export const EMAIL_AUTH_CLOSED_CODE = "email_auth_closed";
